# STRG Design Specification & UI Layout

## 1. Цветовая палитра (Стиль QUIK Dark)
- **Фон графика**: Тёмный графитовый (`#181818`)
- **Сетка**: Приглушенная тонкая (`#2c2c2c`, alpha 15-20%)
- **Шкала цен**: Строго **СПРАВА**, деления с форматированием пробелом (`226 150`)
- **Временная шкала**: Внизу (`10h`, `12h`, `14h`...)
- **Свечи**: Классический монохромный/контурный стиль QUIK (бело-серые бары с фитилями)
- **Фракталы**:
  - Верхние (Up): Зеленые треугольники `▲` (`#26a69a`) над High свечи
  - Нижние (Down): Красные треугольники `▼` (`#ef5350`) под Low свечи
- **Линия ожидания сигнала**: Желтая горизонтальная линия (`#f1c40f`) с ценником на правой шкале
- **Стоп / Тейк**: Красный `#ff5252` (SL) / Зеленый `#00e676` (TP)

## 2. Компоновка экрана (Layout)
- **Центр экрана**: Большой график со свечами (свечи центрированы на экране)
- **Правая колонка**: Вертикальная панель с кнопками и элементами управления
